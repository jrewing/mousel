/**
 * Animation timing constants
 *
 * IMPORTANT: These values must match the CSS custom properties in styles.css:
 * - TRUMP_SHAKE_DURATION (600ms) → --trump-shake-duration (0.6s)
 * - CARD_DEAL_ANIMATION_DURATION (500ms) → --card-deal-animation-duration (0.5s)
 */

// Duration of the trump card shake animation in milliseconds
export const TRUMP_SHAKE_DURATION = 600;

// Delay between dealing each card in milliseconds
export const CARD_DEAL_DELAY = 100;

// Duration of a single card dealing animation in milliseconds
export const CARD_DEAL_ANIMATION_DURATION = 500;
